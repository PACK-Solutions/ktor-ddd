ktor:
  application:
    modules:
      - com.example.ApplicationKt.module
  deployment:
    port: 8080

db:
  url: ${DB_URL:-jdbc:postgresql://localhost:5432/ktor_ddd}
  user: ${DB_USER:-postgres}
  password: ${DB_PASSWORD:-postgres}
